# Bulk upload files

Google Ads' bulk uploader takes a file from disk, a Google Sheet, or an HTTPS
URL. The file picker on this machine is a native dialog that automation cannot
reach, so these are served over HTTPS instead and pointed at from the uploader.

They contain nothing private — campaign structure and the same ad copy that
runs in public. Delete them once the campaign exists.
